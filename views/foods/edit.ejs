<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit <%= food.name %></title>
</head>
<body>
  <h1>Edit <%= food.name %></h1>

  <form action="/foods/<%= food.id %>?_method=PUT" method="POST">
    <label for="name">Name: </label>
    <input type="text" name="name" id="name" value="<%= food.name %>" />

    <label for="isReadyToEat">Ready to eat?</label>
    <input
      type="checkbox"
      name="isReadyToEat"
      id="isReadyToEat"
      <% if (food.isReadyToEat) { %>checked<% } %>
    />

    <button type="submit">Update food!</button>
  </form>

  <a href="/foods">Back to all foods</a>
</body>
</html>
